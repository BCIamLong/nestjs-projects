// * this pattern of import is called barron pattern or Barrel pattern
// * this pattern which is i did before but for now we know that this is Barrel pattern and we can use it more efficient way
// * although it takes a bit of code but it's worth right

export * from './auth.dto';
